actions:
  - name: "Build"
    triggers:
      push:
        branches:
          - "main"
      pull_request:
        branches:
          - "*"
    bazel_commands:
        - "build //..."
  - name: "Deploy"
    triggers:
      push:
        branches:
          - "main"
    bazel_commands:
        - "run :deploy"